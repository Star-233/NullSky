一般指的是 `printf(format, ...)` 里的 `format`
## 格式化字符串写入

这听起来很反直觉，格式化字符串不是用来输出的么？但实际上有这么一个占位符 `%n` ：
- **它的功能**：将“到目前为止已经成功打印的**字符个数**”写入到一个变量中。
- **它的要求**：它对应的参数必须是一个**指针（内存地址）**。

举个简单的例子：
```C
int count = 0;
printf("Hello%n", &count); 
// 运行后，count 的值会变成 5，因为 "Hello" 是 5 个字符。
```

### 怎么控制写入地址？


[[格式化字符串 类canary]]

